name: CI
on:
  push:
    branches: [main, "feat/**"]
  pull_request:

jobs:
  backend:
    name: Backend (vet, build, tests)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: homepad
          POSTGRES_PASSWORD: homepad
          POSTGRES_DB: homepad
        # No host port binding — act_runner exposes services by name on the
        # internal job network (reach it at postgres:5432).
        options: >-
          --health-cmd "pg_isready -U homepad -d homepad"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      DATABASE_URL: postgres://homepad:homepad@postgres:5432/homepad?sslmode=disable
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - name: Vet
        run: go vet ./...
      - name: Build
        run: go build ./...
      - name: Test (against the postgres service)
        run: go test ./... -count=1
